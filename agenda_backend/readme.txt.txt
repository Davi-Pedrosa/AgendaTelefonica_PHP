- instalar dependências:
pip install -r requirements.txt

- executar backend:
python app.py

- acesso navegador:
http://127.0.0.1:5000


endpoints:
GET /contatos → lista todos

POST /contatos → adiciona novo

PUT /contatos/<id> → atualiza

DELETE /contatos/<id> → remove

GET /categorias / POST /categorias